{ "version": 2, "rewrites": [{ "source": "/(.*)", "destination": "/src" }], "builds": [
    { "src": "server.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.ts" }
  ] }